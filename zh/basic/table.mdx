---
title: "表格"
description: "在数据库中创建、导入、分享和管理表格。"
---

{/* mintlify-resync: 2026-05-14 */}
每个数据库可以包含多个表格。表格是用于在数据库中存储相关数据集合的结构化数据存储方式。

每个表格通常包含多行和多列，其中每列代表一个字段及其属性，每行代表一条记录或数据项。表格是数据库的核心组件，用于收集、存储和处理数据，以便后续检索、操作和构建应用。

Teable 的表格等同于数据库中的表，理解这一概念有助于了解 [Teable 与传统电子表格的区别](/zh/compare/teable-vs-excel)，从而更好地掌握 Teable。

### 新建表格

进入目标数据库，点击左侧目录栏顶部的 `+` 按钮，您可以选择：

1. 新建空白表格
2. 从 CSV 文件中导入表格
3. 从 Excel 文件中导入表格

具体操作可参考 [导入](/zh/basic/table/import) 章节。

<img src="/images/ji-ben-gong-neng/8ae8eb8b819f57cc.png" style={{width: "300px", height: "auto"}} className="mx-auto" />

### 修改表格名称

1. 双击左侧目录中的表格名称
2. 输入新名称
3. 点击任意空白处即可自动保存

<img src="/images/ji-ben-gong-neng/678c305b16e3f816.png" style={{width: "300px", height: "auto"}} className="mx-auto" />

### 删除表格

1. 鼠标悬停在目录中的表格名称上，点击右侧的 `...` 按钮打开菜单
2. 点击菜单中的 **删除** 按钮

<img src="/images/ji-ben-gong-neng/1d314b6aef13dc24.png" style={{width: "300px", height: "auto"}} className="mx-auto" />

### 回收站

点击数据库界面左上角的 **回收站** 图标进入表格回收站，可执行以下操作：

> 回收站中的表格数据在物理层面上依然存在，数据库连接仍可访问

<img src="/images/2025-12-01-21-54-44.png" style={{width: "300px", height: "auto"}} className="mx-auto" />

- **恢复**：将表格还原到数据库中，数据完好无损
- **清空**：永久删除所有表格数据，无法再恢复

![](/images/ji-ben-gong-neng/d95c9e75be841b6e.png)

### 设计

点击表格菜单中的 **设计** 按钮，进入表格设计页面，可查看表格的基础属性，包括：

**表格信息**

- 表格所属的数据库 Schema
- 表格名称及其在物理表中的名称
- 表格描述
- 最后修改时间

**字段明细**

| 属性名 | 描述 |
| :--- | :--- |
| **id** | 字段 ID |
| **name** | 字段名称 |
| **dbFieldName** | 字段在物理数据库中的名称 |
| **type** | 字段类型 |
| **description** | 字段描述 |
| **graph** | 查看当前字段依赖关系 |
| **cellValueType** | 当前字段值类型 |
| **isLookup** | 是否为从关联表查找出来的字段 |
| **isMultipleCellValue** | 是否为[数组值字段](/zh/basic/field/common/is-multiple-value) |
| **isComputed** | 是否为计算字段（计算字段的记录值不可修改） |
| **isPending** | 是否为计算中 |
| **hasError** | 是否存在计算错误 |
| **notNull** | 是否启用非空校验 |
| **unique** | 是否启用唯一值校验 |

![](/images/ji-ben-gong-neng/7120f4c7aa1ad8e2.png)

### 分享表格

在表格中点击右上角 **分享**，切换到 **分享表**，打开 **公开分享到网络**，即可生成表格分享链接。

分享表可以配置链接访问权限：

- **可查看**：获得链接的人可以查看表格数据。
- **可编辑**：登录用户可以编辑分享表格中的记录。
- **可另存为副本**：获得链接的人可以把分享内容保存到自己的空间。

还可以配置 **允许查看者复制数据**、**需要密码访问**、重新生成链接、删除分享链接和嵌入配置。

如果只需要分享当前视图，请切换到 **分享视图**。更多信息请参阅 [分享视图](/zh/basic/view/toolbar/share)。

### 访问表格

在 Teable Cloud 中，用户可通过两种方式访问表格：

- [表格视图](/zh/basic/view/grid)
- [API 概览](/zh/api-doc/overview)

视图是最直观的访问方式。用户可通过各种视图对表格进行搜索、操作，并构建复杂的应用。

新添加的表格默认包含[表格视图](/zh/basic/view/grid)，便于用户快速开始处理数据。

### 全局搜索

- 表格支持全局搜索。除了文本类字段外，**日期字段也可以参与全局搜索**，例如直接搜索某个日期值来定位对应记录。
- 当您使用搜索并选择隐藏不匹配的行时，当前结果范围会随搜索条件一起收窄。
- 如果您从附件字段菜单执行批量下载，下载范围也会遵循当前搜索结果，只包含匹配记录中的附件。
- 在附件批量下载弹窗中，可以通过 **附件名前缀** 选择 ZIP 内文件的命名方式：保留默认附件序号、选择某个字段作为前缀，或选择 **无前缀** 保留原始文件名。如果出现重名文件，Teable 会自动追加后缀，保证 ZIP 内文件名唯一。

### 表格功能

用户可以创建各种视图来操作表格，不同视图满足不同场景的需求。例如：表格视图数据密度最高，操作类似电子表格，最易上手；而看板则更好地支持项目管理场景。

- 虽然 Teable 看起来像 Excel，但它更严格。例如，**"数字"列里不能填文字**。这种约束确保了数据规范，是实现高级筛选和自动化的基础。更多细节请看 [Teable vs Excel](/zh/compare/teable-vs-excel)。
- 在删除表格前，请检查它是否通过 **关联字段** 被其他表格引用。如果删除了源头表格，可能会导致关联表格中的数据显示异常。
