---
title: "概要"
description: "プロジェクトを作成、管理、インポート、エクスポートします。.teaファイルを使用して、Teable Cloudのスペース間でデータを移動したり、Cloudとセルフホストのインスタンス間で移行したりできます。"
---

{/* mintlify-resync: 2026-05-26 */}
プロジェクトは、関連するテーブル、アプリ、自動化をまとめる単位です。各スペースには複数のプロジェクトを作成でき、プロジェクトごとにリソースと共同作業の権限を管理できます。

<Note>画面の「プロジェクト」は、API と CLI の `base` リソースに対応します。`baseId` などの API フィールド、ID の接頭辞 `bse`、`/base/...` の URL は変わりません。</Note>

プロジェクトに馴染みがない場合は、Excelのワークブックに似たものと考えてください。各ワークブックには複数のシートを含められます。

## プロジェクトを作成、管理する {#ベースを作成管理する}

### プロジェクトを追加する {#ベースを追加する}

1. スペースを開きます
2. 右上の「プロジェクトを作成」をクリックします

<img
  src="/images/docs/base/2026-05-22-base-create.png"
  alt="プロジェクトを作成"
  className="docs-screenshot"
/>

### テンプレートからプロジェクトを作成する

テンプレートセンターには、そのまま使えるプロジェクトが並んでいます。スペースのサイドバーの **テンプレート**、またはプロジェクト作成時の **テンプレートから** で開けます。テンプレートの詳細で **このテンプレートを使用** をクリックし、配置先のスペースを選ぶと、Teable がテンプレートどおりに新しいプロジェクトを作成します。

一部のテンプレートはソリューションです。オートメーションが外部プラットフォームに接続されて初めてデータを取り続けられるため、必要なプラットフォームがテンプレートの詳細に示されます。この場合、Teable はサンプルデータなしでプロジェクトを作成し、先に設定画面を開きます。

1. プラットフォームごとに 1 行です。**設定** → **統合** ですでに認可済みのアカウントは、画面を開いた時点で紐づきます。残りは **アカウントを接続** で OAuth を実行するか、**自分のものを認可** で既存の接続を使います。
2. API キーが必要な項目は **入力が必要なキー** に並びます。**入力** で新しく作るか、保存済みのシークレットを選びます。
3. **開始** をクリックすると、認証情報がそろったワークフローが有効になり、スケジュール実行のものはすぐに 1 回目が走ります。実行の様子はオートメーションの実行履歴で確認できます。その後、プロジェクトに移動します。

ここで飛ばした統合がなくなるわけではありません。未接続の統合がプロジェクトに残っているあいだは、ページに **N 件の連携が未接続です** と表示され、**接続する** からいつでも補えます。詳しくは[認証情報と連携](/ja/basic/credential)を参照してください。

### プロジェクト名を変更、プロジェクトを削除する {#ベース名を変更ベースを削除する}

1. スペースを開きます
2. プロジェクトにカーソルを合わせます
3. 「···」ボタンをクリックしてメニューを開きます
4. 「名前を変更」または「削除」をクリックします

<img
  src="/images/docs/base/2026-05-22-base-rename-delete.png"
  alt="プロジェクト名を変更またはプロジェクトを削除"
  className="docs-screenshot"
/>

### プロジェクト内のリソースを整理する {#ベース内のリソースを整理する}

プロジェクトを開くと、左側のサイドバーに現在のプロジェクト内のテーブル、アプリ、オートメーションなどのリソースが表示されます。フォルダーを使用してリソースを整理すると、大規模なプロジェクトを閲覧しやすくできます。

一般的な操作は次のとおりです。

- **フォルダーを作成**：左側のサイドバーでフォルダーを作成し、関連するリソースをまとめます。
- **リソースを移動**：テーブル、アプリ、オートメーションをフォルダーへドラッグするか、ドラッグして順序を変更します。
- **階層を整理**：フォルダーには子ノードを含められます。移動しやすいよう、階層を十分に浅く保ちます。
- **説明を追加**：テーブル、アプリ、オートメーションでは、ヘッダーのリソース名の下にある**説明を追加**をクリックします。目的、所有者、引き継ぎ事項、使用規則の記録に説明を使用します。Teableは、リソースを初めて開いたときと、説明の変更後にもう一度、説明を表示します。
- **ノード情報を確認**：リソースの`...`メニューを開き、**ノード情報**を選択すると、フォルダー、テーブル、オートメーション、アプリのIDに加え、作成情報と更新情報を確認できます。

### プロジェクトを別のスペースへ複製する {#duplicate-a-base-to-another-space}

手順1: スペース内で複製するプロジェクトを選択します。

手順2: メニューアイコンをクリックし、「プロジェクトをコピー」オプションを選択します。

手順3: ポップアップで移行先のスペースを選択します。そのスペースの作成者権限が必要です。

大規模なプロジェクトでは、プロジェクト構造、レコード、添付ファイルのコピー中、複製ダイアログに進捗が表示されます。コピーが完了するまでダイアログを開いたままにしてください。変更履歴とコラボレーターはコピーされません。

プロジェクトに、別のスペースを参照するリンク、ルックアップ、ロールアップ、条件付きルックアップ、条件付きロールアップのフィールドが含まれる場合、複製前に影響を受けるフィールドがTeableに表示されます。続行すると、複製したプロジェクトではこれらのフィールドが**一行テキスト**へ変換されます。

<img
  src="/images/docs/base/2026-05-22-base-duplicate.png"
  alt="プロジェクトを別のスペースへ複製"
  className="docs-screenshot"
/>

### スペース間でプロジェクトを移動する {#スペース間でベースを移動する}

必要なスペース権限を持っている場合は、プロジェクトを別のスペースへ移動できます。移動によってリレーションベースのフィールドがスペースをまたいで参照することになる場合、Teableから警告が表示され、影響を受けるフィールドが一覧表示されます。確定すると、移動の一環としてこれらのフィールドが**一行テキスト**へ変換されます。

## プロジェクトをインポート、エクスポートする {#ベースをインポートエクスポートする}

<Card title="あらゆるデータへ接続・移行" icon="sparkles" href="/ja/basic/ai/connect-everything">
  AIチャットを使用して、Airtable、Baserow、NocoDB、SmartSuite、その他のシステムをTeableへ移行します。
</Card>

### プロジェクトをインポートする（データ移行） {#ベースをインポートするデータ移行}

`.tea`ファイル形式では、すべてのテーブル、フィールド、データ、設定を含む完全なプロジェクトをインポートできます。次の用途に便利です。
- **データ移行**：異なるTeableインスタンス間でプロジェクトを移動します（例：Cloudからセルフホストへ）
- **バックアップの復元**：以前にエクスポートしたプロジェクトを復元します
- **テンプレートの共有**：完全なプロジェクト構造を他のチームと共有します

**手順：**
1. スペースを開きます
2. スペースの右上にある「···」ボタンをクリックしてメニューを開きます
3. 「インポート」をクリックします
4. **ファイルからインポート**を選択し、`.tea`ファイルをアップロードします。

<img
  src="/images/docs/base/2026-05-22-base-import.png"
  alt=".teaファイルからプロジェクトをインポート"
  className="docs-screenshot"
/>

<Tip>Airtableからプロジェクトを作成する場合は、同じインポートダイアログで**Airtableからインポート**を選択します。</Tip>

### プロジェクトをエクスポートする（バックアップと移行） {#export-base-backup-&-migration}

バックアップまたは移行のため、プロジェクトを`.tea`ファイルへエクスポートします。エクスポートしたファイルには、すべてのテーブル、フィールド、データ、ビュー、設定が含まれます。

**手順：**
1. スペースを開きます
2. エクスポートするプロジェクトにカーソルを合わせます
3. 「···」ボタンをクリックしてメニューを開きます
4. 「エクスポート」をクリックします
5. レコードデータをエクスポートするには、**レコードを含める**をオンのままにします。構造と設定だけをエクスポートするにはオフにします。
6. **エクスポートを開始**をクリックし、完了するまで待ちます。
7. エクスポートダイアログの**ダウンロード**をクリックして、`.tea`ファイルを保存します。

<img
  src="/images/docs/base/2026-05-22-base-export.png"
  alt="プロジェクトを。teaファイルへエクスポート"
  className="docs-screenshot"
/>

<Note>
- Teableインスタンス間でデータを移行するには、移行元インスタンスからプロジェクトをエクスポートし、移行先インスタンスへ`.tea`ファイルをインポートします。すべてのデータと設定が保持されます。
- エクスポートしたファイルでは、Teableがプロジェクトをまたぐリレーションフィールドを**一行テキスト**フィールドへ変換します。
- スペースをまたいでプロジェクトを複製または移動すると、スペースをまたぐリレーションフィールドは**一行テキスト**へ変換されます。
</Note>

## プロジェクトを共有する {#ベースを共有する}

プロジェクトは公開リンクで共有できます。共有範囲は、プロジェクト全体または現在選択している項目に設定できます。

1. 対象のプロジェクトを開きます
2. プロジェクトまたは選択項目の共有メニューを開きます
3. **Webへ共有**をオンにします
4. 共有リンクまたはQRコードをコピーします

<img
  src="/images/docs/base/2026-05-22-base-share-entry.png"
  alt="プロジェクト共有を開く"
  className="docs-screenshot"
/>

共有範囲は次のとおりです。

| 範囲 | 説明 |
|------|------|
| **プロジェクト全体を共有** | 現在のプロジェクト内のすべての項目を共有します。後から追加した新しいテーブルとフォルダーも自動的に含まれます。|
| **選択した項目を共有** | 現在選択している項目を共有します。フォルダーを共有すると、その子項目も含まれます。|

共有設定では、リンク権限、**閲覧者によるデータのコピーを許可**、パスワードアクセス、リンクの再生成、共有リンクの削除、埋め込み設定のコピーを構成できます。パスワードアクセスをオンにすると、Teableがランダムなパスワードを自動生成します。**リンクとパスワードをコピー**をクリックすれば、まとめて閲覧者に送信できます。詳しくは[共有リンクの保護](/ja/basic/security#共有リンクの保護)を参照してください。

<img
  src="/images/docs/base/2026-05-22-base-share-settings.png"
  alt="プロジェクトの共有設定"
  className="docs-screenshot"
/>

共有範囲にアプリが含まれる場合、公開リンクからアクセスするには、そのアプリを公開する必要があります。

## プロジェクト内の機能 {#ベース内の機能}

プロジェクトには、仕事や業務に関連する情報を記録、整理するための複数のテーブルを含められます。

たとえば、顧客管理プロジェクトでは、「顧客企業」、「顧客連絡先」、「顧客フォローアップレコード」を別々のテーブルにし、会議室管理プロジェクトでは、「会議室管理」、「会議室設備」、「会議室予約」を別々のテーブルにできます。

そのため、プロジェクト内のほとんどの機能はテーブルに関連しています。

* **テーブルを作成**：プロジェクト固有の要件に基づいて新しいテーブルを作成し、テーブル構造、フィールド、データ型を定義できます。
* **テーブルを編集**：プロジェクト内の既存のテーブルを編集し、レコードの追加、削除、変更を行えます。
* **データをエクスポート、インポート**：データのCSVへのエクスポートと、CSVおよびXLSX形式のデータのインポートに対応しています。

テーブルの詳細については、[テーブルのセクション](/ja/basic/table)をご覧ください。
